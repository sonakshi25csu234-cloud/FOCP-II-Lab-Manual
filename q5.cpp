int a, b, c;

    cout<<"Enter value of a: ";
    cin>> a;

    cout<<"\n Enter value of b: ";
    cin>> b;

    // c=a;
    // a=b;
    // b=c;

    // OR

    // a = a + b;
    // b = a - b;
    // a = a - b;

    // ORR

    // a = a * b;
    // b = a / b;
    // a = a / b;

    // ORRR

    // swap(a,b);


    cout<<"Values after swapping are: ";
    cout<< "a = " << a;
    cout<< " b = " << b;